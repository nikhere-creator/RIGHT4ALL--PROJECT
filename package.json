{
  "name": "right4all-backend",
  "version": "1.0.0",
  "description": "Right4All - Backend API Server",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm run build",
    "dev": "cd backend && npm run dev",
    "install-backend": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
